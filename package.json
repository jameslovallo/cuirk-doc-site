{
	"name": "something",
	"author": "James Lovallo",
	"type": "module",
	"dependencies": {
		"browser-sync": "^3.0.2",
		"cuirk": "latest"
	},
	"scripts": {
		"build": "npx build",
		"dev": "browser-sync dist --watch & nodemon -e js,css,html --watch src --watch pages --exec 'npm run build'"
	}
}
